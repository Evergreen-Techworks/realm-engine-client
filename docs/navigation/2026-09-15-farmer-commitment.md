# Farmer objective switching

2026-09-15 Investigated reported loot/combat and mob/mob oscillation after private 1.0.9. Found script-level causes without changing native routing: failed bag actions released ownership but allowed immediate reselection, approaching ordinary boss adds were sorted by distance before a combat lock existed, and a changing preferred target outranked an existing valid combat lock.

2026-09-15 Added a 30-second per-bag retry cooldown after unsuccessful collection, retained ordinary add approach selection for the same encounter while eligible, and made valid combat locks outrank preference changes. Packet death, HP death, targetability, navigability and range still release invalid targets. Explicit boss-guard ordering and vulnerable-boss encounter takeover remain intact. Map reset clears retry and approach state; encounter completion clears approach state. Existing active-bag retention and white-bag boss interruption remain unchanged. This does not claim all possible live-session oscillation is reproduced or that unavailable loot must be collected before proceeding.

2026-09-15 Initial targeted run: 3 failing / 76 passing tests; final `npm test`: 66 files / 660 tests passing, including four new regression tests. Full suite initially exposed a missing getById in a shared-script test fixture; using packet death directly alongside the already available enemy HP avoids adding an unnecessary lookup to candidate selection. `git diff --check` passed. Logs: /tmp/farmer-commitment-red.log and /tmp/farmer-commitment-suite.log. Changes are local source only: private 1.0.9 executable and Windows build source were not modified, no upload or push. Live gameplay and inclusion in the next candidate remain outstanding.
